#include <stdio.h>

int main() {
    
//augmented assignment operators
//used to replace a statement where an operator takes a variable as one of its arguments and then assigns the result back to the same variable x = x + 1
//X+=1
int x = 10;  //ex
//x = x + 2;
//x+=2;
//x = x3;
//x-=3;
4;
    return 0;
}
